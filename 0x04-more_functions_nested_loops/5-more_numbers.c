#include "main.h"

/**
 * more_numbers - prints 10 times the numbers
 * Return: void
 */
void more_numbers(void)
{
	int c;
	int i;

	for (c = 1; c <= 10; c++)
	{
		for (i = 0; i <= 14; i++)
		{
			if (i >= 10)
				_putchar('i');
			_putchar(j % 10 + '0');
		}
		_putchar('\n');
}
